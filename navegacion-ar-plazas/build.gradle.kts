// Los plugins de cada modulo se declaran en el build.gradle.kts del modulo.
// El raiz se deja vacio a proposito para que :nucleo pueda compilarse y testearse
// en un entorno sin Android SDK (por ejemplo un servidor de CI sin `sdkmanager`).
